# provide one or many targets (comma-separated).
TARGETS = WBT
# choose the number of runs for each targets and each run's strategy
N_RUNS = 1 
INIT_STRATEGIES = random
# Optional global tuning
N_TOP_FEATURES = 200
# you can choose to add noise such that y_noise = y + N(0, std(y) * NOISE_STD)
NOISE_STD = 0
N_ITERATIONS = 400000
K_INIT = 1000
MAX_COMPLEXITY = 1000
T_PER_LEVEL = 200
LOSS_THRESHOLD = None
SEARCH_LOSS = mse


Dragon.SPAR_OP_GROUPS = {
    "all": ["select", "unary", "power", "ln", "exp", "sin", "cos", "const"],
    "alg": ["select", "unary", "power", "const"],
    "alg_trig": ["select", "unary", "power", "sin", "cos", "const"],
    "alg_explog": ["select", "unary", "power", "ln", "exp", "const"]
}


# Method-level overrides
description = DragonSR custom runtime on external CSV
parallel_mode = smart
loss_mode = channel
var_aug = false
add_noise = false
 # you can choose the denoiser: lingam, gpr
denoiser = gpr
sampling = true
mc_dropout = falseþ